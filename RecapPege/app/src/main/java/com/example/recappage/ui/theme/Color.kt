package com.example.recappage.ui.theme

import androidx.compose.ui.graphics.Color

// Warna Utama (Brand Color - Orange EatWell)
val OrangePrimary = Color(0xFFFC7100)

// Warna untuk Mode TERANG (Light Mode)
val LightBackground = Color(0xFFFFFFFF) // Putih bersih
val LightSurface = Color(0xFFF5F5F5)    // Abu-abu sangat muda (untuk kartu)
val LightOnBackground = Color(0xFF000000) // Teks Hitam di atas background putih
val LightOnSurface = Color(0xFF1C1B1F)    // Teks Hitam di atas kartu

// Warna untuk Mode GELAP (Dark Mode)
val DarkBackground = Color(0xFF121212)  // Hitam pekat (standar Android)
val DarkSurface = Color(0xFF1E1E1E)     // Hitam agak abu (untuk kartu agar terlihat beda)
val DarkOnBackground = Color(0xFFFFFFFF) // Teks Putih di atas background hitam
val DarkOnSurface = Color(0xFFE6E1E5)    // Teks Putih gading di atas kartu